{
  "name": "pixelmania",
  "version": "1.0.0",
  "description": "Canvas colaborativo en tiempo real — lienzo infinito, territorios, tienda y Discord OAuth",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "start:prod": "node --check server.js && node server.js",
    "check": "node --check server.js && node --check config.js && node --check game-config.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "session-file-store": "^1.5.0",
    "socket.io": "^4.8.1"
  }
}
